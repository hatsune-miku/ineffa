diff --git a/dist/ws/ws-client.js b/dist/ws/ws-client.js
index 1e9d6a27b7ff785e5554e55878be66757470d612..4af363502f755d07a880847291c43086f8203eac 100644
--- a/dist/ws/ws-client.js
+++ b/dist/ws/ws-client.js
@@ -655,7 +655,10 @@ class WsClient {
 exports.WsClient = WsClient;
 const validMessageKinds = new Set(Object.values(message_kind_1.KMessageKinds));
 function isKMessage(value) {
-    if (!isRecord(value) || !validMessageKinds.has(value.s) || !('d' in value))
+    // KOOK documents PONG as { "s": 3 }, without a data field.
+    if (!isRecord(value) || !validMessageKinds.has(value.s))
+        return false;
+    if (value.s !== message_kind_1.KMessageKinds.Pong && !('d' in value))
         return false;
     if (value.sn != null && (!Number.isSafeInteger(value.sn) || value.sn < 0))
         return false;
